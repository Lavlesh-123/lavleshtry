import 'package:flutter/material.dart';

class SeventheVideo extends StatelessWidget {
  const SeventheVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('StateManager'),
      ),body: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Container()
      ],
    ),
    );
  }
}
